default:
  # Trading Instruments
  symbol: "NSE:NIFTY 50" # The index/instrument to analyze
  trade_symbol: "NIFTY25JANFUT" # The symbol to actually trade (Futures) or options prefix
  trade_type: "FUTURES" # or OPTIONS

  # Timeframes
  tf_analysis: "4H" # For AOI
  tf_trend: "1D"    # For Trend
  tf_entry: "5m"    # For Entry

  # Parameters
  aoi_lookback: 200 # 4H candles
  aoi_zone_pct: 0.002 # 0.2% width
  reaction_min_pct: 0.005 # 0.5% move for valid reaction

  # Risk Management
  quantity: 50
  stop_loss_pct: 0.005 # 0.5% fallback SL
  target_pct: 0.015   # 1.5% target (1:3 R:R)

  # Broker
  exchange: "NFO"
  order_type: "MARKET"
  product_type: "NRML"
